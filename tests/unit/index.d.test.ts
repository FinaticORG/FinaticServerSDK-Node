/**
 * Tests for Index.dWrapper.
 * 
 * This file is protected and will not be overwritten during regeneration.
 * Add your test cases here.
 */

import { Index.dWrapper } from '../../src/generated/wrappers/index.d';
import { Index.dApi } from '../../src/generated/api/index.d-api';
import type { Configuration } from '../../src/generated/configuration';

describe('Index.dWrapper', () => {
  let wrapper: Index.dWrapper;
  let mockApi: jest.Mocked<Index.dApi>;
  let config: Configuration;

  beforeEach(() => {
    // TODO: Setup mock API and configuration
    // mockApi = createMockIndex.dApi();
    // config = createMockConfiguration();
    // wrapper = new Index.dWrapper(mockApi, config);
  });

  // TODO: Add test cases
  // Example:
  // describe('index.dMethod', () => {
  //   it('should call API correctly', async () => {
  //     // Test implementation
  //   });
  // });
});
